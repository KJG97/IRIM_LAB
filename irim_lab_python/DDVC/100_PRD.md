# IRIM_LAB PRD

Isaac Sim 확장 **IRIM_LAB**의 제품 요구사항 문서.  
ALLEX 디지털 트윈(Sim2Sim) 및 센서/제어 실험(Sensor Lab) 환경 제공이 목표이다.

---

## 1. 1차 프로젝트 목표

| 목표 | 설명 |
|------|------|
| **통합** | 하위 프로젝트 **sim2sim_test**를 **sim2sim_console**과 통합하여, 메뉴·데이터·실행 경로를 하나의 확장 플로우로 제공한다. |
| **구조 최적화** | sim2sim_test 코드를 모듈화·설정 분리·역할 분리 등으로 구조적으로 최적화한다. |

- sim2sim_console: Sim2Real Debugger, policy·Hydra 연동 등
- sim2sim_test: Sim2Sim 창, scenario·ROS2, UI·이벤트 등

---

## 2. 기능 영역 (요약)

| 영역 | 핵심 기능 |
|------|-----------|
| **Sim2Sim** | 로봇 에셋 로드, LOAD/RESET/RUN, 관절 제어(슬라이더·ROS2), 관절 그룹 표시 on/off, 오버레이, Follow Cube·IK |
| **Sensor Lab** | 센서 테스트 에셋 로드, 카메라·Rigid Body(구 등) 생성, 토크 테스트 UI |
| **ROS2** | Pub/Sub 독립 제어, 통합/개별 토픽, 관절·토크·정책 액션 송수신 |
| **시각화** | Force/Torque, 관절·손 관절 텍스트 오버레이 |

---

## 3. 구현 방향

- **플랫폼**: Isaac Sim Extension (Python), Omniverse Kit 메뉴·UI
- **구조**: `extension.py` → UIBuilder / SensorLabUIBuilder, `scenario.py`(ALLEXDigitalTwin), config·core·ros2·ui 등 모듈 분리 (이벤트 처리 로직은 UIBuilder 내부)
- **설정**: `config/`(joint, ros2, ui, visibility), `global_variables.py`로 제목·상수 관리
- **참고**: Robotics_Study의 scenario+UI 분리, ScrollingWindow·이벤트 구독 패턴

---

## 4. 산출물

- **통합**: sim2sim_console + Sim2Sim
- **Extension 메뉴**: Sim2Sim 창, Sensor Lab 창 (각각 토글)
- **Sim2Sim**: World Controls(LOAD/RESET), Run Scenario(RUN/STOP), Joint Controls, ROS2 Manager, 관절 그룹 체크박스, 오버레이
- **Sensor Lab**: World Controls(LOAD 등), 센서 에셋·카메라·구 생성
- **문서**: DDVC 내 PRD(본 문서), 리팩터링 계획, 기술 스펙·Rules

---

## 5. 성공 기준

- Sim2Sim: LOAD → ROS2 연동 → RUN 시 관절 제어·오버레이 정상 동작
- Sensor Lab: 전용 에셋 로드 및 토크 테스트 UI 동작
- 확장 리로드·창 토글 시 크래시 없음 (extension 방어 코드 유지)

---

## 6. DDVC 문서 구성

| 문서 | 용도 |
|------|------|
| `100_PRD.md` | 본 문서 — 목적·기능·방향·산출물 |
| `101_Refactoring.md` | 리팩터링·최적화 지침 — 원칙, 전략, 체크리스트, 진행 단계 |
| `102_*` | 구조·모듈·기술 스펙 (추후) |
